#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y, z;
    cin >> x >> y >> z;
    
    long long ant = (x - 0) * (x - 0) + (y - 0) * (y - 0) + (z - 0) * (z - 0);

    cout << ant;
    return 0;
}