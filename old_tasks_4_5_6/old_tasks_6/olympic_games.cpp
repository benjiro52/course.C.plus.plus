#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    int gold = 0, silver = 0, bronze = 0;
    cin >> n;

    for (int i = 0; i <= n; i++) {
        for (int j = 0; j <= 3; j++) {
            string country;
            getline(cin, country);
            break;
        }
    }
    
    
}